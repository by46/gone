package image

import (
	"testing"
)

func TestFaceDetect(t *testing.T) {
	//webcam, _ := gocv.VideoCaptureDevice(0)
	//window := gocv.NewWindow("Hello")
	//img := gocv.NewMat()
	//
	//for {
	//	webcam.Read(img)
	//	window.IMShow(img)
	//	window.WaitKey(1)
	//}
}
